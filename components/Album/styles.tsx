import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: 320,
        margin: 10,
    },
    imageUri: {
        width: '100%',
        height: 290,
        borderRadius: 10,
        backgroundColor: 'red'
    },
})

export default styles;