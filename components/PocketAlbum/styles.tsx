import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: 60,
        margin: 10,
    },
    imageUri: {
        width: '100%',
        height: 60,
        backgroundColor: 'red'
    },
})

export default styles;